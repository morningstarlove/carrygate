# 인수인계서 — CARRYGATE

> 이 문서만 읽으면 누구든(혹은 다른 AI 세션이든) 이어서 작업할 수 있게 쓴다.

**최종 갱신:** 2026-09-17
**작업 브랜치:** `claude/analysis-dwawzd`
**저장소:** `morningstarlove/carrygate` (2026-09-17 `signalgate-bridge` 에서 이름 변경)
**프로젝트명:** CARRYGATE — 게이트(신호등) + 캐리(이자) 두 축에서 따왔다.

---

## 1. 지금까지 만든 것

### 마일스톤 1 — 신호등 (완료, 운영 중)

`fetch.py` → `status.json`

업비트에서 6개 코인(BTC/ETH/XRP/TRX/LINK/DOGE)의 일봉 200개를 받아
**지금 시장에 들어가도 되는 때인가**만 판정한다.

- 현재가 > 200일 평균 → 열림
- 현재가 < 200일 평균 → 닫힘
- 3개월 고점 대비 −30% 이하 & 20일선 아래 → 급락 표시

매일 자동 실행되며 `history.jsonl` 에 하루 한 줄씩 쌓인다.

### 마일스톤 2 — 캐리 이자율 (완료, 이번 작업)

`funding.py` → `funding.json`

**전략:** 현물 매수 + 무기한선물 동일 수량 숏 (양쪽 걸기).
가격 방향과 무관하게 원금이 상쇄되고, 펀딩비만 수취한다.
**가격을 맞힐 필요가 없으므로 예측 실패 위험이 없다.**

거래소 5곳에서 펀딩비를 읽어 연환산 수익률로 바꾼다.

| 판정 | 조건 |
|---|---|
| 중단 | 신호등 급락 표시 |
| 진입 가능 | 7일 평균 연 8% 이상 |
| 대기 | 연 2~8%, 또는 7일 평균이 없을 때 |
| 청산 | 연 2% 미만 |

---

## 2. 실제로 확인된 사실 (추측 아님)

2026-09-15 GitHub Actions 실행 결과로 확인한 내용이다.

### 거래소 접속 가능 여부

| 거래소 | GitHub Actions(미국 서버)에서 | 비고 |
|---|---|---|
| 바이낸스 | **차단 (HTTP 451)** | 미국 IP 지역 차단 |
| 바이비트 | **차단 (HTTP 403)** | 미국 IP 지역 차단 |
| OKX | 정상 | |
| 게이트 | 정상 | |
| 하이퍼리퀴드 | 정상 | |

> **중요:** 이건 코드 문제가 아니라 **서버 위치 문제**다.
> 한국에서 직접 돌리면 5곳 다 된다.
> 바이낸스/바이비트 데이터가 꼭 필요해지면 **한국 위치 서버에서 실행**해야 한다.
> (이 문제 때문에 나머지 3곳에서도 7일 평균을 직접 계산하도록 코드를 고쳤다.)

### 왜 7일 평균이 꼭 필요한가

처음 실행에서 같은 날 같은 코인(TRX)이 거래소별로 **연 −33% ~ −65%** 로 벌어졌다.
한 번 찍힌 값(단발값)은 이렇게 튄다. 특히 하이퍼리퀴드는 1시간마다 정산하는데,
그 값을 연 단위로 바꾸면 8,760배가 곱해져 튐이 크게 증폭된다.

그래서 **7일 평균이 없으면 진입 판정을 아예 내리지 않도록** 막아놨다.
(청산 판정은 막지 않는다. 안전한 쪽이기 때문이다.)

### 고친 뒤 실제 결과 (2026-09-15 22:46 UTC, 실행 #28)

3곳 모두 7일 평균이 붙었다(`avg_coverage: 6/6`). 값의 튐이 사라졌다.

| 코인 | 최고 거래소 | 연환산(7일평균) | 3곳 중앙값 |
|---|---|---|---|
| DOGE | hyperliquid | **+10.06%** | +6.10% |
| ETH | hyperliquid | +7.25% | +4.46% |
| BTC | hyperliquid | +6.85% | +6.42% |
| LINK | hyperliquid | +6.56% | +2.50% |
| XRP | hyperliquid | +5.77% | +3.76% |
| TRX | hyperliquid | **−4.77%** | −8.66% |

판정: `진입 가능 — DOGE hyperliquid 연 10.1% (7일평균)`

**같은 날 신호등은 "닫힘"이었다** (BTC 102,543,000원, 200일선 대비 −0.27%).
방향성 매매였다면 전량 현금이지만, 캐리는 가격 방향과 무관하므로 별개로 판정된다.
이 둘이 서로 다른 답을 내는 것이 정상이다.

**확인이 필요한 점:** 6개 코인 전부에서 하이퍼리퀴드가 1등으로 나왔다.
우연이 아닐 가능성이 높으므로(정산 주기 1시간, 펀딩 산식 차이),
실거래 전에 **하이퍼리퀴드에서 실제로 그 이자가 들어오는지 소액으로 확인**해야 한다.
숫자만 높고 실제 체결이 안 되면 의미가 없다.

---

### 마일스톤 3 — 운영 시작·수수료 반영·검산 (완료, 2026-09-17)

**운영 시작.** PR #1 이 `main` 에 병합되어 매일 자동 수집이 돌아간다.
첫 기록은 2026-09-18(한국시간) 자로 저장됐다.

> 주의: 병합 전 이틀(9/16, 9/17)은 `main` 에 캐리 코드가 없어 **신호등만** 기록됐다.
> GitHub 자동 실행은 기본 브랜치(`main`)의 코드만 돌린다. 작업 브랜치에만 있으면
> 아무것도 쌓이지 않는다. 앞으로도 코드를 고쳤으면 `main` 에 병합해야 반영된다.

**수수료를 반영했다 (중요).** 그전까지 보고하던 연 9.2% 같은 수치는 **수수료를 빼기
전** 값이었다. 캐리는 진입 2회·청산 2회로 총 4번 체결되고, 기본 가정(왕복 0.30%,
30일 보유)으로 **연 3.65%**가 빠진다.

| | 총수익 | 순수익 | 판정 |
|---|---|---|---|
| 첫날 DOGE | 연 9.20% | **연 5.55%** | 진입 문턱(8%) **미달** |

즉 **수수료를 넣자 첫날 판정이 "진입 가능"에서 "대기"로 뒤집힌다.** 모든 판정은 이제
순수익 기준이며, 결과에는 총·순 수치가 모두 남는다.

> 수수료율은 거래소 미정 상태의 보수적 추정치다. 거래소를 정하면 `funding.py` 맨 위의
> `TAKER_FEE_SPOT_PCT`(현물), `TAKER_FEE_PERP_PCT`(선물), `HOLD_DAYS`(보유기간)를
> 실제 값으로 바꿔야 한다. 이 셋만 고치면 나머지는 자동 반영된다.

**검산기를 붙였다.** `selfcheck.py` 가 매일 실행 끝에 돌아간다.

- 저장된 연환산을 **원본 펀딩비와 정산주기에서 처음부터 다시 계산**해 대조
- 수수료 차감이 실제로 됐는지 대조
- 대표 거래소가 정말 순수익 1위인지 확인
- 기록 날짜 중복·순서·깨진 줄·빈 구간 검사
- 거래소 수집이 2곳 미만이면 실패 처리(교차 확인 불가)
- 7일 평균 없이 진입 판정이 나오면 실패 처리(안전장치 미작동)

값을 일부러 조작한 9가지 경우로 시험해 전부 잡아내는 것을 확인했다.
문제가 있으면 그날 실행이 **빨간불**로 표시된다.

**자동 점검 스케줄을 걸었다.** 매일 한국시간 오전 10시에 별도 세션이 깨어나
그날 수집이 제대로 됐는지 확인하고, 7일치가 모이면 종합 판단을 보고한다.
보고할 일이 없으면 조용히 끝난다. 알림이 오는 경우는 수집 중단, 검산 실패,
판정 변화, 신호등 변화, 7·14·21일치 도달, 순수익 연 8% 3일 연속 초과다.

**저장소 이름을 `carrygate` 로 바꿨다.** 예전 이름 `signalgate-bridge` 는 GitHub 에
동명 프로젝트가 여럿이라 섞였다. 기존 주소는 GitHub 가 새 주소로 자동 연결하므로
아무것도 깨지지 않는다. 다만 예전 주소로 저장해 둔 링크가 있으면 바꿔 두는 편이 낫다.

---

## 3. 아직 안 한 것 (다음 단계)

### 다음 마일스톤 3 — 실거래 연결

지금은 **판단만 기록**하고 주문은 넣지 않는다. 실거래로 가려면:

1. **거래소 선택** — 한국에서 현물+선물을 한 계정에서 다루려면 바이낸스/바이비트가
   현실적이다. 단, 위 지역 차단 때문에 **실행 서버를 한국에 둬야 한다.**
2. **수수료율부터 실제 값으로 교체** — 거래소를 정하는 즉시 `funding.py` 맨 위
   세 값을 그 거래소의 실제 수수료로 바꾼다. 지금은 보수적 추정치라 실제보다
   불리하게 나올 수 있다.
3. **소액 실전 테스트** — 반드시 최소 금액(10~20만원)으로 1~2주 먼저 돌린다.
   백테스팅보다 소액 실거래가 정확하다. 수수료·호가 차이·출금 지연은
   과거 데이터에 안 남기 때문이다.
4. **반드시 확인할 위험 3가지** (수익률보다 이게 중요하다)
   - **청산 위험** — 선물 쪽 증거금이 부족하면 가격 급등 시 강제 청산된다.
     현물이 있어도 선물만 따로 청산될 수 있다. 증거금을 넉넉히 넣어야 한다.
   - **거래소 위험** — 거래소가 멈추거나 출금을 막으면 양쪽 균형이 깨진다.
     한 거래소에 전액을 두지 않는다.
   - **역펀딩** — 이자가 마이너스로 돌면 **내가 내야 한다.**
     `funding.json` 에서 음수가 나오는 코인이 실제로 있다(위 TRX 사례).

### 그 외 개선 여지

- 실행 주기: 지금은 하루 1회. 펀딩은 8시간마다 정산되므로 8시간 주기가 더 맞다.
- 현물·선물 가격 차이(프리미엄)를 진입 비용으로 반영 — 데이터는 이미 수집 중
  (`premium_pct`), 판정에는 아직 안 쓴다.
- 김치프리미엄(한국 거래소와 해외 거래소 가격 차) — 또 다른 구조적 기회.

---

## 4. 이어서 작업하는 방법

```
브랜치: claude/analysis-dwawzd
```

**결과만 보고 싶을 때 (저장 안 함):**
GitHub 저장소 → Actions 탭 → `CARRYGATE daily` → Run workflow
→ `dry_run` 체크 → 실행. 로그에 코인별 연환산 수익률이 표로 찍힌다.

**코드를 고쳤을 때:** 위와 똑같이 `dry_run` 으로 먼저 돌려 확인한 뒤 합친다.

**핵심 파일:**

| 파일 | 고칠 일이 생기는 부분 |
|---|---|
| `funding.py` 상단 `COINS` | 감시할 코인 추가/제거 |
| `funding.py` 상단 `ENTER_APR` / `EXIT_APR` | 진입·청산 문턱 (연 %) |
| `funding.py` 상단 `AVG_DAYS` | 평균 구간 (기본 7일) |
| `funding.py` `SOURCES` | 거래소 추가/제거 |
| `funding.py` 상단 `TAKER_FEE_*` / `HOLD_DAYS` | **수수료 가정** (거래소 정하면 필수 교체) |
| `selfcheck.py` 상단 `TOL` / `MAX_GAP_DAYS` | 검산 허용 오차 / 기록 공백 경고 기준 |
| `.github/workflows/carrygate.yml` `cron` | 실행 시각 |

**테스트:** 이 저장소는 외부 시세를 받아야 해서 로컬 테스트가 어렵다.
계산 로직은 가짜 데이터로 검증했고(APR 환산, 7일 평균 필터, 판정 분기,
급락 중단, 역펀딩 처리), 실제 데이터 확인은 위 `dry_run` 실행으로 한다.

---

## 5. 원칙 (지켜야 할 것)

1. **이 저장소는 주문을 넣지 않는다.** API 키를 넣지 않는다. 읽기 전용을 유지한다.
   실거래는 별도 저장소/서버로 분리한다.
2. **7일 평균 없이 진입 판정을 내리지 않는다.** 단발값은 튄다.
3. **수익률은 항상 수수료를 뺀 뒤에 말한다.** 총수익만 보면 실제보다 부풀려 보인다.
4. **실데이터로 확인한 것만 사실로 적는다.** 위 "확인된 사실"은 모두 실행 로그 근거다.
